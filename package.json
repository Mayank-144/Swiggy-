{
  "name": "swiggy-clone-mern",
  "version": "1.0.0",
  "description": "Full-stack Swiggy Clone built with MERN Stack, Tailwind CSS, and Framer Motion",
  "scripts": {
    "start": "node server/server.js",
    "server": "node server/server.js",
    "client": "npm run dev --prefix client",
    "dev": "npx -y concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build"
  }
}
